# Rust build artifacts
/target/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.dylib
*.dll
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# maturin build output
*.whl

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# macOS debug symbol bundles (generated by maturin develop)
*.dSYM/

# uv lock file for tools (not a project dependency lock)
uv.lock

# macOS
.DS_Store

# Cargo.lock is committed for binary projects, not for libraries.
# Since py-dantzigpy produces a binary artifact (cdylib), we keep it.
Cargo.lock
